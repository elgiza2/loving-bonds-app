<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch animation: theme-colored screen -> mark shrinks next to the
     "Megsy" wordmark -> the lockup grows while the brand color floods in. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/splashRoot"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:clickable="true"
    android:focusable="true"
    android:background="@color/app_background"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent">

    <View
        android:id="@+id/splashFlood"
        android:layout_width="600dp"
        android:layout_height="600dp"
        android:layout_gravity="center"
        android:alpha="0"
        android:background="@drawable/brand_flood" />

    <LinearLayout
        android:id="@+id/splashLockup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/splashMark"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_megsy_mark" />

        <TextView
            android:id="@+id/splashWord"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="10dp"
            android:paddingEnd="2dp"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:text="Megsy"
            android:textSize="44sp"
            android:letterSpacing="-0.04"
            android:fontFamily="@font/space_grotesk"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
