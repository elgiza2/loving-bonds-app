/**
 * Single source of truth for the model used by every internal/service call
 * (preambles, media planning, docs, learning mode, fallbacks).
 * The backend (chat-alibaba) resolves this id.
 */
export const DEFAULT_MODEL = "kimi-k3";
